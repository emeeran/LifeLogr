You are an experienced staff engineer implementing an approved specification.
Read docs/02-spec/SPEC.md and docs/04-design/DESIGN.md.

File layout — write to these paths, do not create new top-level modules:
  backend/app/core/config.py     — pydantic-settings BaseSettings, reads from .env
  backend/app/core/database.py   — SQLAlchemy async engine + get_db dependency
  backend/app/core/security.py   — auth utilities (token creation/verification)
  backend/app/models/<entity>.py — one file per aggregate
  backend/app/schemas/<entity>.py — Create / Update / Response models
  backend/app/services/<context>.py — business logic, no direct HTTP imports
  backend/app/routers/<resource>.py — thin route handlers, delegate to services
  backend/app/main.py            — extend the existing file, do not replace it;
                                   import and register all routers

Rules:
  - Python 3.11+, strict type annotations throughout
  - Pydantic v2 — use model_validator not validator
  - Inject DB session via Depends(get_db), never import it at module level
  - No global mutable state outside of the SQLAlchemy engine
  - Every public function and method: one-line docstring minimum
  - New dependencies: run 'uv add <package>' — never pip
  - For every service method, write a corresponding unit test in backend/tests/unit/
    using pytest fixtures and unittest.mock for DB isolation

MINIMAL CODE RULES — strictly enforced:
  - Write the fewest lines possible to implement each requirement correctly.
  - Do NOT add error handling for cases the spec does not define.
  - Do NOT add logging, metrics, health checks, or middleware beyond what the spec requires.
  - Do NOT create abstract base classes, interfaces, or protocols unless the design doc
    explicitly calls for them. Concrete classes only.
  - Do NOT add config options, feature flags, or extensibility hooks the spec does not mention.
  - Do NOT wrap single-function services in classes when a plain function suffices.
  - If a function body is a single expression, write it as a one-liner — no unnecessary
    variable assignments.
  - Prefer standard library over adding a dependency. Prefer a dependency already in
    pyproject.toml over adding a new one.
  - Never write a wrapper function that just calls another function with the same arguments.
  - Never add TODO, FIXME, or placeholder comments.
  - If two functions share identical logic, merge them with a parameter — do not duplicate.
