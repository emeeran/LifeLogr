services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/backend/prod.db
      - ./media:/app/backend/media
    env_file:
      - backend/.env
    environment:
      - APP_ENV=production
    restart: unless-stopped
