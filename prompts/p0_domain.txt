You are a Domain-Driven Design expert. Analyse raw_idea.txt and produce two files:

docs/00-domain/DOMAIN.md
  - Bounded contexts: name, responsibility, owned data
  - Ubiquitous language glossary: every domain term with a precise one-line definition
  - Core aggregates: name, invariants (rules that must never be violated), root entity
  - Mark every assumption or gap with ⚠️ NEEDS_CLARIFICATION: <question>

docs/00-domain/CONTEXT_MAP.md
  - One Mermaid diagram showing all bounded contexts
  - For each context boundary, state the integration pattern:
      Shared Kernel | Customer-Supplier | Conformist | Anti-Corruption Layer | Open Host
  - One sentence justifying each pattern choice

No code. No implementation details. Concepts only.
